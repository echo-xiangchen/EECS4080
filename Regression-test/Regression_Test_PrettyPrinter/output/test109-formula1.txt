(declare-const i Int)
(assert (= i 1))
(declare-const j Int)
(assert (= j 2))
(assert (not (= (* i j) (+ j 5))))
(check-sat)
;Remove the comment if the result of z3 online tool returns "sat"
;(get-model)
